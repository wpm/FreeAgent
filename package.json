{
  "name": "freeagent-viewers",
  "version": "1.0.0",
  "private": true,
  "description": "Root JavaScript workspace for FreeAgent's TypeScript viewers (apps/*/viewer).",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "schemas:json": "uv run python -m freeagent.schema && uv run python -m twentyquestions.schema",
    "schemas:ts": "pnpm --filter twentyquestions-viewer run generate",
    "schemas": "pnpm run schemas:json && pnpm run schemas:ts",
    "typecheck": "pnpm --recursive run typecheck"
  }
}
